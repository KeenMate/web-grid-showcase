name: UAT (Plebeius) build
on:
  push:
    branches:
      - uat
jobs:
  build:
    uses: KeenMate/gha-shared/.github/workflows/deploy-docker-image.yml@master
    secrets: inherit
    with:
      image-name: web-grid-showcase
      image-tag: uat
      deployment-host: web-03.km8.vm
      deployment-dir: web-grid-showcase-uat
